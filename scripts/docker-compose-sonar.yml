version: "3"
services:
  sonar:
    build:
      context: ../
      dockerfile: scripts/Dockerfile.sonarcloud
    container_name: sonarcloud
    hostname: sonarcloud
    volumes:
      - ..:/workspace
    command:
      - |
        /workspace/scripts/run-sonar.sh